using ErrorOr;
using MediatR;
using {{ .Service.Name }}.Application.Common;
using {{ .Service.Name }}.Application.{{ .Entity.FeatureName }}.Dtos;
using {{ .Service.Name }}.Application.{{ .Entity.FeatureName }}.Interfaces;
using {{ .Service.Name }}.Domain.Entities;
{{ if .Entity.HasValueObjectFields }}using {{ .Service.Name }}.Domain.ValueObjects;
{{ end }}

namespace {{ .Service.Name }}.Application.{{ .Entity.FeatureName }}.Queries.List;

public sealed class List{{ .Entity.Name }}QueryHandler(I{{ .Entity.Name }}Repository repository) : IRequestHandler<List{{ .Entity.Name }}Query, ErrorOr<PagedResult<{{ .Entity.Name }}Dto>>>
{
    public async Task<ErrorOr<PagedResult<{{ .Entity.Name }}Dto>>> Handle(List{{ .Entity.Name }}Query request, CancellationToken cancellationToken)
    {
        var normalized = PaginationPolicy.Normalize(request.Page, request.PageSize);
        var result = await repository.ListAsync(normalized.Offset, normalized.PageSize, cancellationToken);
        return new PagedResult<{{ .Entity.Name }}Dto>(result.Items.Select(ToDto).ToList(), normalized.Page, normalized.PageSize, result.TotalCount);
    }

    private static {{ .Entity.Name }}Dto ToDto(EntitySnapshot<{{ .Entity.Name }}> snapshot) => new(
{{ range $index, $field := .Entity.Fields }}{{ if $index }},
{{ end }}        snapshot.Entity.{{ $field.ValueAccess }}{{ end }}{{ range .Entity.RelationshipScalarFields }},
        snapshot.Entity.{{ .ValueAccess }}{{ end }},
        snapshot.ConcurrencyToken);
}
